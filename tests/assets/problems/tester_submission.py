from gap import problem


@problem()
def tester_submission() -> None:
    """This is just a single function that does nothing."""


__problem_config__ = {
    "is_script": False,
    "check_stdout": False,
    "mock_input": False,
    "captured_context": (),
}
